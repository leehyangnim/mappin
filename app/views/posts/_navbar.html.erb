
<nav class="navbar navbar-default" style= "box-shadow: 0 2px 4px 0 rgba(0,0,0,0.16),0 2px 10px 0 rgba(0,0,0,0.12)!important;">
  <div class="container-fluid" style="background-color: #fb7038">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag('logo6.png', class: "logo hvr-float-shadow"), posts_path  %>
      <!-- <a class="navbar-brand" href="/posts">map'pin</a> -->
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1" style="color: white;">
        <% if user_signed_in? %>
      <ul class="nav navbar-nav navbar-right">
          <li> <div class="name"> Hello, <%= current_user.email %></div> </li>
          <%= render 'notifications/notification_dropdown' %>
          <li div="signout"><%= link_to "Sign Out", sessions_path, method: :delete %></li>
      </ul>
        <% end %>
      </div>
</div>
</nav>


<!-- <%= link_to "All Posts", posts_path %>

<% if user_signed_in? %>
  Hello <%= current_user.email %>
  |
  <%= render 'notifications/notification_dropdown' %>
  <%= link_to "Sign Out", sessions_path, method: :delete %>
<% end %>

<hr>

<% if flash[:notice] %>
  <div class="flash-notice"><%= flash[:notice] %></div>
<% elsif flash[:alert] %>
  <div class="flash-alert"><%= flash[:alert] %></div>
<% end %> -->
