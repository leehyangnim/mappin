
// if ($('.comments#post_11').width() == 660) {
//   var newCommentHtml = "<%= j(render('/comments/show_comment', {comment: @comment})) %>"
// } else {
//   var newCommentHtml = "<%= j(render('/comments/comment', {comment: @comment})) %>"
// }
<% post = @comment.post %>
$('.comments#<%= "post_#{post.id}" %>').html('<%= j render "comments", {comments: @comments} %>');

<% comment = Comment.new %>
$('.comment-form#<%= "post_#{post.id}" %>').html('<%= j render "form", {comment: comment, post: post} %>')
