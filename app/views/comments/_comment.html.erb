<% post = comment.post %>
<div>
  <%= comment.body %>
  <%= link_to "Delete", post_comment_path(post, comment),
                        method: :delete,
                        remote: true,
                        data:   {confirm: "Are you sure?"}%>
  <hr>
</div>
